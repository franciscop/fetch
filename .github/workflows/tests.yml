name: tests

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
      - name: Install Bun
        uses: oven-sh/setup-bun@v1
      - name: Install dependencies
        run: npm install
      - name: Build it
        run: npm run build
      - name: Run the linter
        run: npm run lint
      - name: Run the tests
        run: npm test
        env:
          CI: true
